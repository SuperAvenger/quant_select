下面是一份**可直接交给 Codex CLI 开发的完整开发文档**（PRD + 技术方案 + 文件清单 + YAML Schema + 关键撮合规则 + 交付验收）。你把这整段复制给 codex cli 当任务说明即可。

------

# A股趋势启动量化选股回测器（TuShare Pro）开发文档（交给 Codex CLI）

## 1. 目标

用 **TuShare Pro API** 构建一个**最小可跑的日频事件驱动回测框架**，包含两类策略：

- **策略一：趋势启动（momentum_breakout）**
- **策略二：超跌反弹（mean_reversion_rebound）**

- **指数趋势过滤**（Regime Filter）：指数 close > MA200 才允许开新仓
- **个股选股**：52周新高接近度 + 12-1 动量 打分 → TopN
- **入场条件（启动确认）**：突破 + 放量 + 趋势均线
- **出场条件**：MA 止损 或 ATR 追踪止损
- **撮合模型**：信号日收盘生成订单，**次日开盘 next-open** 撮合
- **A股关键约束**：停牌不可成交、涨跌停不可成交（保守/乐观模式）、T+1
- 输出：净值曲线、交易记录、简单绩效指标

> 数据源必须基于 https://tushare.pro/

------

## 2. 数据接口（TuShare Pro 必用接口）

实现并封装以下接口（参考 TuShare 文档）：

- `trade_cal`：交易日历（回测日期序列）
- `stock_basic`：A股列表/上市日期/市场/行业等（股票池）
- `pro_bar`：日线行情（open/high/low/close/vol/amount，支持复权 qfq/hfq/none）
- `stk_limit`：每日涨跌停价格（up_limit/down_limit）
- `suspend_d`：每日停复牌（停牌不可成交）

**重要**：涨跌停规则不要自己算，直接依赖 `stk_limit` 输出的 up/down limit。

------

## 3. 项目目录结构（必须按此拆文件）

```text
quant_a/
  pyproject.toml
  requirements.txt
  README.md

  configs/
    momentum_breakout.yaml

  src/
    quant_a/
      __init__.py

      config/
        loader.py
        schema.md

      data/
        tushare_client.py
        panel_builder.py
        store.py              # 预留：本地缓存（可先空实现）
        universe.py           # 预留：扩展过滤（可先合在panel_builder里）

      features/
        indicators.py
        precompute.py

      strategy/
        base.py               # 可先空
        momentum_breakout.py  # 核心策略：make_signal_fn
        mean_reversion_rebound.py  # 第二策略：超跌反弹

      broker/
        matcher.py            # Order/Fill + next-open 撮合 + A股规则
        costs.py              # 可先合并在matcher里（最小版）
        models.py             # 可先空（最小版先写在matcher）

      backtest/
        engine.py             # 事件驱动引擎
        metrics.py            # 可先在cli里算（最小版）
        report.py             # 可选

      cli/
        run.py                # 入口：python -m quant_a.cli.run --config ...
```

------

## 4. YAML 配置文件：Schema & 示例

### 4.1 Schema（在 `src/quant_a/config/schema.md` 写清楚）

必需字段：

- `tushare.token_env`：token 的环境变量名（禁止明文写 token）
- `backtest.start/end`：YYYYMMDD（end 可用 `auto`，自动取“前一交易日”）
- `data.adj`：qfq/hfq
- `benchmark.ts_code`：基准指数，如 000300.SH
- `strategy.name`：momentum_breakout / mean_reversion_rebound
- `strategy.rebalance`：M（月频）/ D（日频）
- `strategy.top_n`
- `strategy.regime_filter.enabled/index_ts_code/ma_days`
- `strategy.ranking.use_52w_high/use_mom_12_1/w_52w_high/w_mom_12_1`
- `strategy.entry.breakout_lookback/volume_ratio_min/trend_ma_days`
- `strategy.exit.use_ma_stop/ma_stop_days/use_atr_trailing/atr_days/atr_k`
- `strategy.entry.ret_5_max/ret_20_max/rsi_max/rebound_lookback`
- `strategy.exit.mean_revert_ma_days/stop_loss_pct/take_profit_pct/max_hold_days`
- `strategy.ranking.use_ret_5/use_ret_20/w_ret_5/w_ret_20`
- `execution.fill_price`：next_open
- `execution.limit_rule.enabled/mode`：conservative / optimistic
- `execution.t_plus_one`
- `execution.slippage_bps/commission_bps/stamp_tax_bps`
- `execution.max_turnover`
- `risk.weight_scheme/max_weight_per_name`

可选字段（常用）：

- `backtest.resume` / `resume_from` / `resume_lookback_days`
- `data.pull_mode` / `incremental` / `pull_workers` / `pull_rate_per_min`
- `data.quality_check.enabled`
- `execution.validation.enabled`
- `execution.commission_bps_by_prefix` / `stamp_tax_bps_by_prefix` / `min_commission`

### 4.2 示例配置：`configs/momentum_breakout.yaml`

```yaml
tushare:
  token_env: TUSHARE_TOKEN
  timeout: 30

backtest:
  start: "20160101"
  end: "auto"
  calendar_exchange: "SSE"
  resume: false
  resume_from: "outputs/runs/last"
  resume_lookback_days: 504

data:
  adj: "qfq"
  cache_dir: "./.cache"
  store_format: "parquet"
  pull_mode: "by_trade_date"
  incremental: true
  refetch_empty: true
  pull_workers: 32
  pull_prefetch: 96
  pull_retries: 3
  pull_retry_sleep: 2.0
  pull_rate_per_min: 500
  quality_check:
    enabled: true

benchmark:
  ts_code: "000300.SH"

universe:
  exclude_st: true
  min_list_days: 120
  exclude_suspended: true
  min_avg_amount_20: 30000000

strategy:
  name: "momentum_breakout"
  rebalance: "M"
  top_n: 30

  regime_filter:
    enabled: true
    index_ts_code: "000300.SH"
    ma_days: 200

  ranking:
    use_52w_high: true
    use_mom_12_1: true
    w_52w_high: 0.6
    w_mom_12_1: 0.4

  entry:
    breakout_lookback: 120
    volume_ratio_min: 1.5
    trend_ma_days: 120

  exit:
    use_ma_stop: true
    ma_stop_days: 60
    use_atr_trailing: true
    atr_days: 20
    atr_k: 3.0

execution:
  fill_price: "next_open"
  limit_rule:
    enabled: true
    mode: "conservative"
  t_plus_one: true
  slippage_bps: 10
  commission_bps: 1.454
  commission_bps_by_prefix:
    "510": 16.0
    "511": 16.0
    "512": 16.0
    "513": 16.0
    "515": 16.0
    "516": 16.0
    "517": 16.0
    "518": 16.0
    "560": 16.0
    "563": 16.0
    "588": 16.0
    "589": 16.0
    "159": 16.0
    "160": 16.0
    "161": 16.0
    "162": 16.0
    "163": 16.0
    "164": 16.0
    "165": 16.0
    "168": 16.0
    "169": 16.0
  min_commission: 5.0
  stamp_tax_bps: 10
  stamp_tax_bps_by_prefix:
    "510": 0.0
    "511": 0.0
    "512": 0.0
    "513": 0.0
    "515": 0.0
    "516": 0.0
    "517": 0.0
    "518": 0.0
    "560": 0.0
    "563": 0.0
    "588": 0.0
    "589": 0.0
    "159": 0.0
    "160": 0.0
    "161": 0.0
    "162": 0.0
    "163": 0.0
    "164": 0.0
    "165": 0.0
    "168": 0.0
    "169": 0.0
  max_turnover: 0.30

risk:
  weight_scheme: "equal"
  max_weight_per_name: 0.05
```

------

## 5. 核心策略定义（momentum_breakout）

### 5.1 预计算特征（写回 panel）

对每只股票按时间序列计算并添加列：

- `ma_trend = SMA(close, trend_ma_days)`
- `ma_stop = SMA(close, ma_stop_days)`
- `atr = ATR(high,low,close, atr_days)`
- `mom_12_1 = ret(252) - ret(21)`
- `nh_52w = close / rolling_max(close,252)`
- `breakout = close > rolling_max(high, breakout_lookback)`
- `vol20 = mean(vol, 20)`
- `vol_ratio = vol / vol20`

### 5.2 指数过滤（Regime Filter）

若启用：只有当 `index_close > SMA(index_close, ma_days)` 时允许开新仓。否则只允许卖出不允许买入。

### 5.3 选股与入场（调仓日）

- 仅在调仓日执行（最小版：月频）
- 候选过滤（启动确认）：
  - `close > ma_trend`
  - `breakout == True`
  - `vol_ratio >= volume_ratio_min`
- 在候选集合上做截面打分：
  - `score = w_52w_high * zscore(nh_52w) + w_mom_12_1 * zscore(mom_12_1)`
- 选 TopN 进入目标持仓（等权）

### 5.4 出场（每天都检查）

对持仓股票：

- MA 止损：`close < ma_stop` → SELL
- ATR 追踪止损：维护 `highest_close_since_entry`
  若 `close < highest_close_since_entry - atr_k*atr` → SELL

### 5.5 组合构建 + 换手限制

- 目标权重：TopN 等权，但单票不超过 `max_weight_per_name`
- 从 `cur_w -> tgt_w` 计算换手：`turnover = 0.5 * sum(|tgt-cur|)`
- 若 turnover > `execution.max_turnover`：使用线性缩放
  `tgt = cur + alpha*(tgt-cur)`，alpha = max_turnover/turnover

### 5.6 订单生成

- 估值价格用信号日 `close` 估算订单数量（整数股）
- A股最小化：以 100 股为一手（lot=100），qty 向下取整到 100 的倍数
- 下单顺序：**先卖后买**（资金回笼）

------

### 5.7 第二策略定义（mean_reversion_rebound）

**定位**：抓超跌后的均值回归反弹，日频调仓，短期持有。

**预计算特征（写回 panel）**

- `ret_5 = ret(close, 5)`
- `ret_20 = ret(close, 20)`
- `rsi_14 = RSI(close, 14)`
- `low_20 = rolling_min(low, 20)`
- `ma_revert = SMA(close, mean_revert_ma_days)`
- `volatility = rolling_std(ret(close), 20)`（可选）

**入场条件（每日）**

- `ret_5 <= ret_5_max`（如 -0.08）
- `ret_20 <= ret_20_max`（如 -0.15）
- `rsi_14 <= rsi_max`（如 30）
- `close <= low_20 * (1 + rebound_lookback)`（如 0.02）

**截面排序**

- `score = w_ret_5 * zscore(-ret_5) + w_ret_20 * zscore(-ret_20)`
- 可叠加低波过滤：`score += w_low_vol * zscore(-volatility)`

**出场条件（每天都检查）**

- 均值回归止盈：`close >= ma_revert` 或 `>= entry_price*(1+take_profit_pct)`
- 固定止损：`close <= entry_price*(1-stop_loss_pct)`

**调仓频率**

- 建议 `rebalance: D`（日频），TopN 等权。

**配置示例（简化版）**

```yaml
strategy:
  name: "mean_reversion_rebound"
  rebalance: "D"
  top_n: 30
  ranking:
    use_ret_5: true
    use_ret_20: true
    w_ret_5: 0.6
    w_ret_20: 0.4
  entry:
    ret_5_max: -0.08
    ret_20_max: -0.15
    rsi_max: 30
    rebound_lookback: 0.02
  exit:
    mean_revert_ma_days: 20
    stop_loss_pct: 0.08
    take_profit_pct: 0.10
```

------

## 6. 撮合模型（A股最关键坑：涨跌停不可成交）

### 6.1 交易时序（避免未来函数）

- 在 t（日）收盘后生成订单
- 在 t+1（日）开盘价撮合：`fill_price = next_open`

### 6.2 停牌不可成交

- 若 `suspend_d` 显示该日停牌，则该日所有订单不可成交

### 6.3 涨跌停不可成交（只用日线能做到的最可靠近似）

依赖 `stk_limit` 的 `up_limit` / `down_limit`

支持两种模式（YAML 切换）：

**(1) conservative（默认推荐，回测不虚高）**

- BUY：若 `open >= up_limit` → 不成交
- SELL：若 `open <= down_limit` → 不成交

**(2) optimistic**

- BUY：仅当 `open==high==low==up_limit`（一字涨停）才不成交
- SELL：仅当 `open==high==low==down_limit`（一字跌停）才不成交

### 6.4 T+1

- 当日买入的股票不能当日卖出
  （在 next-open 模型下：同一 exec_date 不能同时 BUY 后 SELL；实现为：若 last_buy_date == exec_date，则拒绝 SELL）

### 6.5 费用与滑点

- 滑点：bps（买加、卖减）
- 佣金：bps（双边）
- 印花税：卖出 bps（单边）

撮合后：

- BUY：现金减少 `price*qty + fee`
- SELL：现金增加 `price*qty - fee`

------

## 7. 回测引擎（事件驱动最小闭环）

循环遍历交易日 dates：

1. 用信号日 d 的数据生成 orders（exec_date=next_d）
2. 在 next_d 用 matcher 撮合（停牌/涨跌停/T+1/费用/滑点）
3. 更新持仓/现金
4. 用 next_d 的 close 估值净值，记录 equity curve

输出：

- `equity_curve.csv`：date,equity,cash,n_pos
- `trades.csv`：ts_code,side,qty,price,exec_date,fee,reason
- `metrics.json`：年化、波动、最大回撤、交易笔数（最小版）
- `trade_summary.json`：成交/拒单统计与原因
- `rebalance_stats.csv`：调仓候选统计
- `rejections.csv`：拒单明细（如有）
- `data_quality.json`：数据质量诊断（开启 data.quality_check 时）
- `execution_validation.json`：撮合规则验证（开启 execution.validation 时）
- `closed_trades.csv`：成交闭环明细
- `yearly_stats.csv`：年度收益与回撤
- `monthly_win_rate.csv`：按月胜率
- `industry_win_rate.csv`：按行业胜率
- `state.json`：续跑状态（resume 用）

------

## 8. CLI 使用方式（必须实现）

命令：

```bash
export TUSHARE_TOKEN="你的token"
python -m quant_a.cli.run --config configs/momentum_breakout.yaml --out outputs/runs/test01
```

行为：

- 读取 YAML & token env
- 拉交易日历、股票列表、行情、涨跌停、停牌
- 构建 panel（multiindex: trade_date, ts_code）
- 预计算特征
- 运行回测
- 输出 csv/json 到 --out 目录

------

## 9. 交付验收（Definition of Done）

**功能验收**

-  能在给定区间跑通（至少 3 年数据）
-  输出核心文件：equity_curve.csv / trades.csv / metrics.json（并附带 trade_summary.json、rebalance_stats.csv）
-  交易记录里能看到被拒单（停牌/涨跌停）导致的“未成交”情况（至少在日志/统计里体现，可配合 execution_validation.json）
-  切换 `limit_rule.mode` 能显著改变成交率与回测结果（证明规则生效）
-  `regime_filter.enabled=false` 时允许熊市开仓（结果变化明显）

**质量验收**

-  不使用未来数据：信号用 d，成交用 next_d open
-  使用 `stk_limit` 作为涨跌停判定依据
-  停牌来自 `suspend_d`
-  代码按模块拆分，不在 cli.run 里堆业务逻辑

------

## 10. Codex 实现顺序（强制按这个排，最快跑通）

1. YAML loader（token env）
2. TuShareClient（5个接口）
3. universe + panel_builder（拼出 panel）
4. indicators + precompute_features
5. matcher（含 conservative/optimistic）
6. backtest engine（能跑出 trades/equity）
7. strategy.make_signal_fn（实现打分/调仓/退出/换手限制）
8. CLI 输出 + metrics

------

## 11. 代码文件清单（Codex 需要创建/填充）

必须实现（最小可跑）：

- `src/quant_a/config/loader.py`
- `src/quant_a/data/tushare_client.py`
- `src/quant_a/data/panel_builder.py`
- `src/quant_a/features/indicators.py`
- `src/quant_a/features/precompute.py`
- `src/quant_a/broker/matcher.py`
- `src/quant_a/backtest/engine.py`
- `src/quant_a/strategy/momentum_breakout.py`
- `src/quant_a/cli/run.py`
- `configs/momentum_breakout.yaml`
- `requirements.txt` / `pyproject.toml` / `README.md`

可延后（v2）：

- `data/store.py`（增量更新/断点续跑进一步完善）
- `backtest/metrics.py`（更完整指标、分段回测）
- 行业中性化/市值中性化、行业权重上限

------

# 备注（给 Codex 的注意事项）

1. TuShare 有频率/积分限制：建议实现简单缓存（v2）
2. A股涨跌停/停牌导致的“不可成交”是回测真实性核心，必须严格执行
3. next-open 撮合是为了避免未来函数，禁止当天收盘成交
4. lot=100 的简化是最小版；科创板/创业板也多为100股一手（按需要后续再细化）

------

如果你希望我再加一段“Codex CLI 一次性指令模板”（例如：让它创建文件、逐文件填充、最后运行冒烟测试），我也可以把 **codex 的多步 prompt** 给你，直接一键生成整个仓库。
